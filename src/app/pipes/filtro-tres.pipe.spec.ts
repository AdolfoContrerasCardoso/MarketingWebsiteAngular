import { FiltroTresPipe } from './filtro-tres.pipe';

describe('FiltroTresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTresPipe();
    expect(pipe).toBeTruthy();
  });
});
