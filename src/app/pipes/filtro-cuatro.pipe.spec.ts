import { FiltroCuatroPipe } from './filtro-cuatro.pipe';

describe('FiltroCuatroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCuatroPipe();
    expect(pipe).toBeTruthy();
  });
});
