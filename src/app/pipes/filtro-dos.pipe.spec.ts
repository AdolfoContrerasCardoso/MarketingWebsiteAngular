import { FiltroDosPipe } from './filtro-dos.pipe';

describe('FiltroDosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDosPipe();
    expect(pipe).toBeTruthy();
  });
});
